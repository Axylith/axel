# .github/workflows/test.yml
name: AXLE Tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: g++ -std=c++20 -I src tests/axl_test.cpp -o axl_test
      - run: ./axl_test